# Local git config
.gitconfig.local

# Local fish config
config.local.fish

# Fish variables (system-specific and auto-generated)
fish_variables

# Homebrew Bundle lockfile
homebrew/.config/homebrew/Brewfile.lock.json

# Lazy.nvim lockfile
nvim/.config/nvim/lazy-lock.json
